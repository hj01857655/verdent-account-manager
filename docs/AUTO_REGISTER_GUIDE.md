# Verdent AI 自动注册与账户管理系统

## 功能特性

### 1. 自动注册系统
- ✅ 多线程并发注册 Verdent AI 账号
- ✅ 集成 tempmail.plus 临时邮箱 API
- ✅ 自动处理人机验证(shadow DOM)
- ✅ 支持批量注册(1-10个账号)
- ✅ 可配置并发数和运行模式

### 2. 账户管理界面
- ✅ 圆弧卡片式账户展示
- ✅ 显示注册时间、邮箱、过期时间、剩余额度
- ✅ 一键复制 Token、邮箱、密码
- ✅ 刷新账户信息(获取最新额度)
- ✅ 编辑账户信息
- ✅ 删除账户
- ✅ JSON 文件本地存储

### 3. 登录管理
- ✅ 一键登录并打开 VS Code
- ✅ PKCE 认证流程
- ✅ 自动保存 Token

### 4. 存储管理
- ✅ 重置设备身份
- ✅ 完全清理所有数据
- ✅ VS Code ID 管理

## 安装依赖

### Python 依赖
```bash
pip install -r requirements.txt
```

### Rust 依赖
```bash
cd Verdent_account_manger/src-tauri
cargo build
```

### 前端依赖
```bash
cd Verdent_account_manger
npm install
```

## 使用说明

### 1. 获取 TempMail API Key
访问 https://tempmail.plus/ 注册并获取 API Key

### 2. 启动应用
```bash
cd Verdent_account_manger
npm run tauri dev
```

### 3. 自动注册账号
1. 点击"账户管理"标签页
2. 点击右上角"➕ 自动注册"按钮
3. 输入 TempMail API Key
4. 设置注册数量和并发数
5. 点击"开始注册"

### 4. 管理账户
- **复制 Token**: 点击卡片中的 📋 按钮
- **刷新信息**: 点击 🔄 按钮更新账户额度
- **编辑账户**: 点击 ✏️ 按钮修改信息
- **删除账户**: 点击 🗑️ 按钮删除

## 文件结构

```
Verdent/
├── verdent_auto_register.py          # Python 自动注册脚本
├── verdent_auto_login.py             # Python 登录脚本
├── requirements.txt                   # Python 依赖
├── Verdent_account_manger/
│   ├── src/
│   │   ├── App.vue                   # 主应用
│   │   └── components/
│   │       ├── AccountCard.vue       # 账户卡片组件
│   │       └── AccountManager.vue    # 账户管理组件
│   └── src-tauri/
│       └── src/
│           ├── account_manager.rs    # 账户管理模块
│           ├── commands.rs           # Tauri 命令
│           └── lib.rs                # Rust 入口
```

## 技术栈

- **前端**: Vue 3 + TypeScript
- **后端**: Rust + Tauri 2.0
- **自动化**: DrissionPage
- **邮箱**: tempmail.plus API
- **存储**: JSON 文件

## 注意事项

1. **人机验证**: 当前仅支持模拟点击,不使用第三方验证码破解服务
2. **注册频率**: 建议控制并发数,避免被检测
3. **API Key**: 请妥善保管 TempMail API Key
4. **数据备份**: 账户数据存储在 `~/.verdent_accounts/accounts.json`

## 常见问题

### Q: 注册失败怎么办?
A: 检查 TempMail API Key 是否有效,降低并发数重试

### Q: 如何备份账户数据?
A: 复制 `~/.verdent_accounts/accounts.json` 文件

### Q: 人机验证无法通过?
A: 关闭无头模式,手动点击验证框

## 开发者

Verdent Team
